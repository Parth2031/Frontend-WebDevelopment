// TODO:: Working of a Namespace File :-
// ? We can also assign Namespace Keyword with Same Name in Different Files ->

namespace Maths 
{
  export function calculateRectangle(length: number, breadth: number): number {
    return length * breadth;
  }
}
